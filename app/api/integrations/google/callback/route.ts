import { cookies } from "next/headers";
import { NextResponse } from "next/server";
import { requirePermission } from "@/lib/crm/supabase-server";
import { exchangeGoogleCode,googleEmail,saveGoogleConnection } from "@/lib/crm/gmail";
export async function GET(request:Request){const base=process.env.NEXT_PUBLIC_APP_URL?.replace(/\/$/,"")||new URL(request.url).origin,url=new URL(request.url),store=await cookies(),expected=store.get("mj_google_oauth_state")?.value;store.delete("mj_google_oauth_state");if(url.searchParams.get("error"))return NextResponse.redirect(`${base}/admin/integrations?google=cancelled`);const code=url.searchParams.get("code"),state=url.searchParams.get("state");if(!code||!state||!expected||state!==expected)return NextResponse.redirect(`${base}/admin/integrations?google=state_error`);const session=await requirePermission("manage_business_settings");if(!session)return NextResponse.redirect(`${base}/admin/login`);try{const token=await exchangeGoogleCode(code),email=await googleEmail(token.access_token);await saveGoogleConnection(session.token,session.user.id,token,email);return NextResponse.redirect(`${base}/admin/integrations?google=connected`);}catch{return NextResponse.redirect(`${base}/admin/integrations?google=error`);}}